# Phase 40 Apply Instructions

1. Keep a clean, backed-up working copy of `C:\Users\hp\Desktop\KAYAD-main`.
2. Extract this package somewhere outside the repository.
3. Run `APPLY_PHASE_40_TO_REPO.cmd` from the extracted package.
4. From the repository, run `node scripts/validate-phase40.mjs`.
5. Run the frontend lint/build and backend Jest suite before committing.
6. Review `git diff` and `git status`.
7. Commit and push only after the validation and tests pass.
