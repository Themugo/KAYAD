import { useEffect } from 'react';

// PWA update banner - simplified version without virtual:pwa-register
// The full PWA functionality can be enabled by installing vite-plugin-pwa

export default function SWUpdateBanner() {
  useEffect(() => {
    // PWA update handling would go here
    // For now, this is a placeholder that renders nothing
  }, []);

  return null;
}
