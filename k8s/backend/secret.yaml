apiVersion: v1
kind: Secret
metadata:
  name: kayad-secrets
  namespace: kayad
type: Opaque
stringData: {}  # Secrets managed externally — apply via: kubectl create secret generic kayad-secrets -n kayad --from-literal=supabase-url=... --from-literal=jwt-secret=...
