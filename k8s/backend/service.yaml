apiVersion: v1
kind: Service
metadata:
  name: kayad-backend
  namespace: kayad
  labels:
    app: kayad-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: kayad-backend
