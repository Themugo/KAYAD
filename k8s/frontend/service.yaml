apiVersion: v1
kind: Service
metadata:
  name: kayad-frontend
  namespace: kayad
  labels:
    app: kayad-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: kayad-frontend
